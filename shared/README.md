# Shared

Common data models and helpers used by more than one pod (e.g. `Request`, `Location`, `Resource` objects from Pod 3).

**Caution zone:** everyone depends on this folder. Keep changes small, announce them in the group chat before opening a PR, and get a fast review. See `CONTRIBUTING.md`.
